class Booking {
  constructor(surfer, host, duration, location) {
    this.surfer = surfer
    this.host = host
    this.duration = duration
    this.location = location
  }
}

module.exports = Booking